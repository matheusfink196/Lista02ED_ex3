/*3. Construir uma função recursiva que receba um vetor e seu tamanho e apresente a quandade de
números pares existentes no vetor. Considere que a entrada deve ser, apenas de números naturais
diferentes de zero.*/
package controller;

public class vetController {
	public vetController() {
		super();
	}
	
	public int contagem(int tam, int vet[]) {
		if (tam==0) {
			return 0;
		}else {
			if (vet[tam] % 2 == 0) {
				
			}
				
			}
	}
}